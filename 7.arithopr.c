#include<stdio.h>

int main() {
    int a,b;
    printf("Enter a ");
    scanf("%d",&a);

    printf("Enter b ");
    scanf("%d",&b);

    printf("sum is : %d ", a + b);
    /*for substraction,multiplication,division use -,*,/ in place of + in the above printf function */

return 0;

}